---
tags:
  - guide
  - task
---

A task is an operational work item that takes 1-7 days, contributes to a parent goal, and has a binary done/not-done outcome. It states *what* will be true when done plus the *concrete steps* to get there.

## TL;DR

- **Use for**: operational work, 1-7 days, binary done/not-done
- **Create**: `/vault-cli:create-task "<title>"`
- **Audit**: `/vault-cli:audit-task "<title>"`
- **Sections**: Summary → Impact → Success Criteria → [DoD] → **Out of Scope** → Tasks → Progress
- **Forcing functions**: ≥ 1 parent goal linked; Out-of-Scope enumerated; criteria binary

## Goal

Produce a task page that is atomic, binary-completable, parent-goal-linked, and auditable by `task-auditor` without ambiguity.

## When to Write a Task

| Situation | Task? |
|-----------|------|
| Operational obligation (SHOULD do), 1-7 days | Yes |
| Concrete change with clear completion | Yes |
| Contributes to a parent goal's success criterion | Yes |
| Personal ambition, weeks-to-months effort | No — write a goal |
| Multi-task umbrella outcome | No — write a goal |
| Recurring routine (daily/weekly) | Yes, but use `recurring:` frontmatter |
| Code change inside an active goal | Yes; consider whether a dark-factory spec is needed instead |

## Creating a Task

Use the slash command:

```
/vault-cli:create-task "<title>"
```

The command invokes `task-creator`, which scaffolds a file in the configured vault's `tasks_dir` (typically `24 Tasks/`). The agent reads vault config via `vault-cli config list --output json` — never hardcode paths.

## Task Structure

### Frontmatter

```yaml
---
status: todo
page_type: task
priority: 3                                      # 1 (highest) – 3
category: <domain>                               # optional
goals:                                           # ≥ 1 parent goal recommended
  - "[[Parent Goal]]"
assignee: bborbe                                 # who's holding the baton
task_identifier: <uuid>                          # generated by tooling
planned_date: 2026-MM-DD                         # optional
defer_date: 2026-MM-DD                           # optional, for snoozing
recurring: weekly                                # optional, for routine tasks
---
```

`status` valid values: `in_progress`, `todo`, `backlog`, `hold`, `completed`, `aborted`.

`assignee` semantics: empty (`""`) means **unclaimed inbox** (anyone with vault access can pick up); an agent name means the executor should spawn that agent; a human name means that human is currently doing the work.

### Required sections

In order:

1. `Tags: [[Task]]` (after frontmatter, before content separator)
2. **Summary** — first paragraph after the `---` separator. Action-verb-led. 1-2 sentences.
3. `# Impact` — why this matters; reference parent goal's success criteria explicitly when possible
4. `# Success Criteria` — 2-4 binary checkbox outcomes (binary = done or not done)
5. `# Definition of Done` — required for complex tasks (≥ 4 success criteria, multi-phase, or ambiguous terms). Per-criterion verification: `**Automated**: ...`, `**Behavioral**: ...`
6. `# Out of Scope` — recommended; explicit deferrals (parallels `# Non-goals` on goals)
7. `# Tasks` — actionable subtask checkboxes (work-tracking; not parent-goal links)
8. `# Progress` — log of work done, dated entries; lives at bottom

Optional: `# Related` — sister tasks, docs, runbooks.

### Out of Scope — the scope-creep guard

Adopted by parallel with goals' Non-goals convention. Explicit deferrals prevent task bloat:

**Good out-of-scope:**

```markdown
# Out of Scope

- Renaming the agent's CRD (separate concern)
- Migrating existing tasks in the vault (only new emissions affected)
- Schema changes in `bborbe/agent/lib`
```

**Bad out-of-scope:**

```markdown
# Out of Scope

- Future improvements
- Anything else
```

**Rules:**
- 2-5 items typical for tasks (less than goals; tasks are smaller)
- Each item is a *concrete* deferral or alternative
- Link a follow-up task if the deferred work is real

## Scope Check

Before committing to a task, verify these signals:

- **Success criteria count 2-4** — if ≥ 5, complex enough to need DoD
- **Single mental model** — if the task touches multiple unrelated domains/repos, split
- **Days-to-week effort** — if multi-week, it's a goal masquerading as a task
- **Linked to ≥ 1 parent goal** — orphan tasks are scope creep by definition
- **Title is action-verb-led** — "Add X", "Fix Y", "Migrate Z"; not "Stuff about X"

If 3+ smells fail → split or promote to a goal.

## Preflight Checklist

Before approving:

- [ ] What outcome will be true when done (binary)?
- [ ] Does this contribute to a specific parent goal's success criterion?
- [ ] What's explicitly out of scope?
- [ ] Are success criteria binary (yes/no, not "improve X")?
- [ ] Is this 1-7 days of work, not weeks?
- [ ] Is the title action-verb-led?

## Audit

Always audit before committing publicly:

```
/vault-cli:audit-task "<task title or path>"
```

The auditor (`task-auditor` agent) checks structure, success-criteria binary-ness, parent-goal linkage, DoD presence for complex tasks, and Out-of-Scope clarity.

## Lifecycle

| Status | Meaning | Trigger to enter |
|--------|---------|------------------|
| `todo` | Defined, not started | Task file created with required sections filled |
| `in_progress` | Actively working | `/vault-cli:work-on-task` or `assignee` set + first subtask started |
| `hold` | Blocked | `blocked_by:` field populated, or operator sets manually |
| `completed` | All success criteria met | `/vault-cli:complete-task` — checks every `# Success Criteria` checkbox is `[x]` |
| `aborted` | Abandoned without completion | Operator sets manually with reason in body |
| `backlog` | Not committed yet | Initial state before commitment |

Recurring tasks reset on `complete` rather than archive — `recurring:` frontmatter drives the deferral cycle.

## Vault-Specific Examples

This doc covers structure and conventions. For concrete examples (good vs weak tasks, vault-quality assessment, common mistakes), see the per-vault writing guide:

- Personal vault: `~/Documents/Obsidian/Personal/50 Knowledge Base/Task Writing Guide.md`

That guide is example-rich and references real tasks; this doc is the generic contract.

## References

- `goal-writing.md` — tasks roll up to goals
- `/vault-cli:task-status` — progress on an active task
- `/vault-cli:verify-task` — mechanical structure check
- `/vault-cli:defer-task` — snooze a task

**When to also write a dark-factory spec** (in addition to the task):

| Condition | Action |
|---|---|
| Code change is multi-prompt (3+ prompts) | Spec required |
| Architectural decision worth recording | Spec required |
| Single-file fix, behavior obvious | Prompt only — no spec |
| Config/yaml change, no code | Direct edit — no spec, no prompt |
