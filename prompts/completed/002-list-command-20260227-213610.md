<objective>
Add a `vault-cli list` command that lists tasks from the vault, filterable by status.
</objective>

<context>
Project: ~/Documents/workspaces/vault-cli (Go CLI, cobra, Obsidian markdown)
Read CLAUDE.md and existing code before making changes.

Vault folder structure (configured per vault in ~/.vault-cli/config.yaml):
- Tasks: "24 Tasks" (Personal) / "40 Tasks" (Brogrammers)
- Task files have YAML frontmatter with `status` field: todo, in_progress, backlog, completed, hold, aborted

Existing commands: complete, defer, update
Existing storage: Storage interface in pkg/storage/markdown.go
Existing domain: Task struct in pkg/domain/task.go
</context>

<requirements>
1. Add `ListTasks` method to Storage interface and markdownStorage:
   - Scan tasks_dir for all *.md files
   - Parse frontmatter for each
   - Return []*domain.Task

2. Add `list` operation in pkg/ops/list.go:
   - Interface: ListOperation with Execute(ctx, vaultPath) error
   - Filter by status if --status flag provided
   - Default: show only `todo` and `in_progress` tasks
   - Output: one task per line, format: `[status] Task Name`

3. Add `list` subcommand in pkg/cli/cli.go:
   - `vault-cli list` — show todo + in_progress tasks
   - `vault-cli list --status in_progress` — filter by status
   - `vault-cli list --all` — show all statuses

4. Add mock for ListOperation via counterfeiter comment

5. Write Ginkgo tests for list operation (pkg/ops/list_test.go)
</requirements>

<implementation>
- Follow Interface → Constructor → Struct → Method pattern (see go-patterns.md)
- Follow Ginkgo/Gomega test patterns (see go-testing.md)
- Follow git-workflow.md: create feature branch, CHANGELOG under ## Unreleased, PR
- Output is plain text to stdout, one task per line
- Sort output: in_progress first, then todo, alphabetical within each group
</implementation>

<verification>
1. `make test` passes
2. `make precommit` passes
3. Build: `go build -mod=mod -o /tmp/vault-cli .`
4. Test: `/tmp/vault-cli list` against Personal vault — should show current tasks
5. Test: `/tmp/vault-cli list --status in_progress`
6. Test: `/tmp/vault-cli list --all`
</verification>

<success_criteria>
- Storage.ListTasks implemented
- ListOperation with status filtering
- `vault-cli list` shows todo + in_progress by default
- make test passes
- make precommit passes
- PR created
</success_criteria>
