---
status: completed
---

<objective>
Add `done` → `completed` to the status migration map in `vault-cli lint --fix`.

Currently `status: done` is reported as a non-fixable INVALID_STATUS error. It should be auto-migrated to `completed` when `--fix` is passed, following the same pattern as `next`→`todo` and `current`→`in_progress`.
</objective>

<context>
Go CLI project for managing Obsidian vault tasks.
Read CLAUDE.md for project conventions.

Key file: `./pkg/ops/lint.go`

The migration map already exists in two places:
1. `detectInvalidStatus()` — `statusMigrationMap` determines if a value is fixable
2. `fixInvalidStatus()` — regex and map determine the replacement value

Both need `"done": "completed"` added.
</context>

<requirements>
1. Add `"done": "completed"` to `statusMigrationMap` in `detectInvalidStatus()`
2. Update the regex in `fixInvalidStatus()` to also match `done`: change `(next|current)` to `(next|current|done)`
3. Add `"done": "completed"` to `statusMigrationMap` in `fixInvalidStatus()`
4. Add test cases in `lint_test.go`:
   - `done` status → detected as INVALID_STATUS, Fixable=true
   - `--fix` on `done` → replaces with `completed` in file
</requirements>

<output>
Modify in place:
- `./pkg/ops/lint.go`
- `./pkg/ops/lint_test.go`
</output>

<verification>
```
make test
go run main.go lint 2>&1 | grep "done"
```

Confirm:
- `make test` passes
- `status: done` shows as WARN (fixable) not ERROR
- `go run main.go lint --fix` changes `done` → `completed` in files
</verification>

<success_criteria>
- `make test` passes
- `done` is fixable (WARN not ERROR)
- `lint --fix` writes `completed` to files with `status: done`
</success_criteria>
