---
status: completed
---

<objective>
Fix `vault-cli list --all` to show ALL tasks with no status filtering.

Currently `--all` only shows `todo`, `in_progress`, `done`, `deferred` — missing `backlog`, `completed`, `hold`, `aborted`. When `--all` is passed, no filtering should happen at all.
</objective>

<context>
Go CLI project for managing Obsidian vault tasks.
Read CLAUDE.md for project conventions.

Key file: `./pkg/ops/list.go`

The `showAll` branch currently builds a hardcoded map of 4 statuses. It should instead skip filtering entirely and pass all tasks through.
</context>

<requirements>
1. In `Execute()` in `./pkg/ops/list.go`, when `showAll == true`, skip the status filter entirely — all tasks pass through
2. Update `./pkg/ops/list_test.go` to add a test: `--all` returns tasks of every status including `backlog`, `completed`, `hold`, `aborted`
</requirements>

<implementation>
Change the `showAll` branch from building a hardcoded map to simply skipping filtering:

```go
if showAll {
    // No filtering — show everything
} else if len(statusFilter) > 0 {
    ...
}

// Then in the filter loop:
var filteredTasks []*domain.Task
for _, task := range tasks {
    if showAll || statusesToShow[task.Status] {
        filteredTasks = append(filteredTasks, task)
    }
}
```
</implementation>

<output>
Modify in place:
- `./pkg/ops/list.go`
- `./pkg/ops/list_test.go`
</output>

<verification>
```
make test
go run main.go list --all 2>&1 | grep "backlog\|completed\|hold\|aborted" | head -5
```
</verification>

<success_criteria>
- `make test` passes
- `vault-cli list --all` shows tasks of all statuses including backlog, completed, hold, aborted
</success_criteria>
