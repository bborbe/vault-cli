<objective>
Add --status filter flag to all list commands in vault-cli.
After this change: `vault-cli task list --status in_progress` shows only tasks with that status.
Applies to: task list, goal list, theme list, objective list, vision list.
</objective>

<context>
Go CLI project at ~/Documents/workspaces/vault-cli.
Read CLAUDE.md for project conventions.
Read ~/.claude/docs/go-patterns.md for interface/constructor patterns.

All list commands are created by the generic factory function `createGenericListCommand` in `./pkg/cli/cli.go`.
The domain types (Task, Goal, Theme, etc.) all have a `Status` field in their frontmatter.
Status values used in practice: todo, in_progress, completed, hold, aborted, backlog.
</context>

<requirements>
1. Add `--status` flag to `createGenericListCommand` in `./pkg/cli/cli.go`:
   - Flag name: `--status` (string, default: "" meaning no filter)
   - When empty: show all items (existing behaviour)
   - When set: show only items where frontmatter `status` field matches the value (case-insensitive)

2. The filtering must happen after reading pages from storage, before printing.
   Each listed item already has a Status field — filter on that.

3. The flag must work with the existing `--vault` and `--assignee` flags (independent filters, all applied together).

4. Apply to ALL entity list commands that use `createGenericListCommand`:
   - `vault-cli task list --status in_progress`
   - `vault-cli goal list --status in_progress`
   - `vault-cli theme list --status in_progress`
   - `vault-cli objective list --status in_progress`
   - `vault-cli vision list --status in_progress`

5. When multiple vaults are active and one vault has no results after filtering, still print the vault header but no rows (consistent with existing multi-vault behaviour).
</requirements>

<implementation>
Read `createGenericListCommand` in `./pkg/cli/cli.go` before implementing — understand the current iteration and print pattern.

The status filter should be applied like the existing assignee filter:
```go
if statusFilter != "" && !strings.EqualFold(string(item.Status), statusFilter) {
    continue
}
```

Flag registration pattern (follow --assignee as reference):
```go
cmd.Flags().StringVar(&statusFilter, "status", "", "filter by status (e.g. in_progress, todo, completed)")
```
</implementation>

<constraints>
- Only modify `./pkg/cli/cli.go` — no changes to storage or domain packages needed
- Do NOT break existing behaviour when --status is not set
- Case-insensitive match (in_progress == In_Progress)
- Do NOT run make precommit — use make test iteratively; run make precommit once at the very end
</constraints>

<verification>
Run: `make test`

Manual spot checks:
- `vault-cli task list` → all tasks (no filter)
- `vault-cli task list --status in_progress` → only in_progress tasks
- `vault-cli task list --status todo --assignee bborbe` → both filters applied together
- `vault-cli goal list --status in_progress` → only in_progress goals
</verification>

<success_criteria>
- make test passes
- --status flag works on all five entity list commands
- Combined with --assignee and --vault flags correctly
- No regression in existing list behaviour
</success_criteria>
