---
status: completed
spec: [029-bug-workon-resume-drops-operator-into-headless-tail]
summary: Added bare slash-command auto-invoke classification to /vault-cli:execute-task with allowlist boundary and explicit disqualifiers; updated CHANGELOG.md
execution_id: vault-cli-resume-gate-exec-177-spec-029-execute-task-subtask-classification
dark-factory-version: v0.192.9
created: "2026-08-09T09:15:00Z"
queued: "2026-08-09T09:36:21Z"
started: "2026-08-09T09:47:31Z"
completed: "2026-08-09T09:49:27Z"
---

<summary>
- When a task's first open subtask is nothing but a slash command, `/vault-cli:execute-task` now runs it instead of printing it and waiting.
- Every other kind of subtask is printed exactly as it is today — printing stays the default and the safe side.
- Only first-party commands can be auto-run; a fixed allowlist decides, not the operator's attention at read time.
- Shell commands, `make` targets, file paths, and URLs are never auto-run, even when a subtask looks like an instruction to run one.
- A subtask that mixes a command with other prose, passes shell metacharacters as arguments, or names two commands is printed rather than run.
- If a named command does not actually resolve, it is printed instead of run — an unknown name never becomes a guess.
- Task files are not always operator-written (recurring-task generators, synced vaults), so the boundary is written down as a rule the command follows, not a convention readers are expected to honor.
- Markdown-only change; no Go code is touched.
</summary>

<objective>
Give `/vault-cli:execute-task` an executable classification rule for the first unchecked subtask: a bare slash-command call from a fixed allowlist is invoked directly in the session; anything else is printed as today. This closes the second half of the spec — a task whose subtask is `Run \`/start-day\`` currently prints the command instead of running it, so `vault-cli task work-on` still costs an approval turn even after the resume fix lands.
</objective>

<context>
Read `/workspace/CLAUDE.md` for project conventions (note: `commands/*.md` ship as part of the plugin, so a changelog entry is required) and `/workspace/docs/dod.md`.

This prompt is markdown-only and independent of prompts 1 and 2 — it touches no Go file.

Read these files fully before making changes:

- `/workspace/commands/execute-task.md` — the file you are changing. Verified structure:
  - Frontmatter: `description`, `argument-hint`, `allowed-tools: [Read, Edit, Glob, Bash, AskUserQuestion, Task]`
  - `## Process` with `### 1. Resolve task path` … `### 7. Surface first subtask + DoD (always — final output)`
  - Step 7 currently parses "First unchecked `- [ ]` checkbox under `# Tasks`" and prints:
    ```
    🎯 Start with: <first unchecked subtask text, truncated to ~120 chars>

    📋 When done, verify:
    - <DoD bullet 1>
    ```
  - `## Notes`, then `## Integration`, whose final block is a list beginning `Output ends with one of:` — the first item is `` `🎯 Start with: <subtask>` + `📋 When done, verify: <DoD>` (gate passed or idempotent re-entry) ``.

- `/workspace/commands/work-on-task.md` — read the *Phase 5 — Auto-chain* section for the house style of "invoke a command from inside a command":
  ```
  Invoke `Skill: vault-cli:plan-task "<name>"`.
  ```
  Its frontmatter is `allowed-tools: [Task, AskUserQuestion, Skill, Bash(vault-cli *)]` — note `Skill` is listed. Do NOT edit this file.

- `/workspace/CHANGELOG.md` — prompts 1 and 2 add a `## Unreleased` section with one `- fix:` bullet about the resume path. You add a **second** bullet to that same section for this (genuinely distinct) user-visible change. If `## Unreleased` does not exist yet, create it below the preamble block (the `All notable changes…` line and the three `* MAJOR / MINOR / PATCH` bullets) and above the newest `## vX.Y.Z` heading.

Read this coding-plugin doc (present in the container at this path):
- `/home/node/.claude/plugins/marketplaces/coding/docs/agent-command-development-guide.md` — slash-command definition conventions.
- `/home/node/.claude/plugins/marketplaces/coding/docs/changelog-guide.md` — changelog entry format.

**Resolved ambiguity — read before writing the rule.** The spec's Security section says "a subtask whose entire content is a single slash-command token", while its own reproduction uses the subtask `- [ ] Run \`/start-day\`` — a leading `Run ` verb and backticks. Taken literally, the spec's boundary would reject the spec's own reproduction. Resolution used below, and it is the rule you implement: normalization strips an optional leading `Run `/`Invoke ` verb and surrounding backticks **before** classification; what remains must be exactly one allowlisted command token plus optional quoted plain-text arguments. Every other form of prose still disqualifies. This satisfies both the reproduction and the allowlist boundary.
</context>

<requirements>
1. In `/workspace/commands/execute-task.md`, add `Skill` to the frontmatter `allowed-tools` list so the command can invoke another command:

   ```yaml
   allowed-tools: [Read, Edit, Glob, Bash, AskUserQuestion, Task, Skill]
   ```

   Change nothing else in the frontmatter except the `description`, per requirement 6.

2. Add exactly one new section heading, spelled character-for-character `### Subtask classification`, at the start of a line, placed inside `## Process` immediately **after** the end of `### 7. Surface first subtask + DoD (always — final output)` and **before** `## Notes`. There must be exactly one line in the file matching `^### Subtask classification`.

3. Write the section body. It must contain these literal strings verbatim (they are grepped by the spec's acceptance criteria) — `bare slash-command call → invoke`, `anything else → print`, `never auto-invoke`, and `disqualifies`. Use this content:

   ```markdown
   ### Subtask classification

   Step 7 classifies the first unchecked subtask before it decides what to do with it. Exactly two outcomes, and printing is the default:

   - **bare slash-command call → invoke** — run the command in this session, immediately, with no approval turn and no `AskUserQuestion` gate.
   - **anything else → print** — emit the `🎯 Start with:` line and stop, exactly as before.

   **Normalization.** Before classifying, take the subtask text (everything after the `- [ ] ` marker), then: trim whitespace; strip an optional leading `Run ` or `Invoke ` verb (case-insensitive); strip surrounding backticks. So `` Run `/start-day` `` and `/start-day` normalize to the same token. Nothing else is stripped — this list is exhaustive by design, and matches the spec's Security / Abuse section verbatim.

   Residue after normalization means print, not invoke. `` Run `/start-day`. `` keeps a trailing `.`, fails the single-token check, and is printed. That fallback is deliberate: erring toward print is always safe, erring toward invoke is not.

   **Bare slash-command call.** The normalized text is a bare slash-command call when *all* of these hold:

   1. It starts with `/`.
   2. It is a single command token, optionally followed by arguments.
   3. The command token matches the allowlist below.
   4. Every argument is plain text or a double-quoted string containing no shell metacharacters.

   **Allowlist** — first-party Claude Code commands only:

   - `/vault-cli:*`
   - `/dark-factory:*`
   - `/coding:*`
   - bare-name commands and skills shipped by those same plugins (for example `/start-day`, `/update-task`)

   Everything else is outside the boundary and is **never auto-invoke**d — print it instead. That includes shell commands, `make` targets, `bash …`, file paths, URLs, and commands from any other plugin or marketplace. The allowlist is what bounds blast radius, not the reader's attention: task files are not always operator-authored (`recurring-task-creator` generates them from YAML, and vaults sync across machines).

   **Disqualifiers.** Any one of the following disqualifies a subtask from auto-invoke — print it instead:

   - prose surrounding the command beyond the stripped `Run `/`Invoke ` verb, e.g. `Run /start-day and check the output` or `Before standup, /start-day`
   - shell metacharacters in an argument: `` ` ``, `$`, `|`, `&`, `;`, `>`, `<`, `(`, `)`, newline
   - a second command on the same line, e.g. `/plan-task then /execute-task`
   - a command token that does not resolve when invoked — an unknown name is printed, never guessed at or approximated

   When in doubt, print. A printed subtask costs the operator one keystroke; a wrongly invoked one costs a command execution they did not choose.

   **How to invoke.** Print the DoD block first (so the destination stays visible), then print `🚀 Running: <normalized command>` and invoke it with `Skill: <command without the leading slash> "<args, if any>"` — the same form `/vault-cli:work-on-task` Phase 5 uses. The invoked command's own output becomes the tail of this command's output; do NOT re-print `🎯 Start with:` afterwards.
   ```

4. Make step 7 actually consume the rule rather than only defining it. In `### 7. Surface first subtask + DoD (always — final output)`, after the "Parse the task file" bullets and before the `Print:` block, insert:

   ```markdown
   Apply **Subtask classification** (next section) to the first unchecked subtask. If it classifies as a bare slash-command call, print the DoD block, then `🚀 Running: <command>`, then invoke the command and stop — the invoked command's output is the final output. Otherwise fall through to the print form below.
   ```

   After this edit `grep -c 'Subtask classification' commands/execute-task.md` must return at least `2` — the section heading plus this reference from the step that prints `🎯 Start with:`.

   The two resulting output shapes, so there is no ambiguity about what the invoke branch omits:

   ```
   # classifies as a bare slash-command call — no 🎯 line
   📋 When done, verify:
   - <DoD bullet 1>
   🚀 Running: /start-day
   <the invoked command's own output follows and is the final output>

   # anything else — unchanged from today
   🎯 Start with: <subtask text>

   📋 When done, verify:
   - <DoD bullet 1>
   ```

5. In `## Integration`, extend the final `Output ends with one of:` list with the new terminal outcome, keeping the existing items unchanged:

   ```markdown
   - `🚀 Running: <command>` followed by that command's own output (first unchecked subtask was a bare allowlisted slash-command call)
   ```

6. Update the frontmatter `description` and the `## Notes` section to reflect the new behavior:
   - `description:` — append `; auto-invokes a bare allowlisted slash-command subtask` to the existing description text, keeping the rest verbatim.
   - Add one `## Notes` bullet:
     ```markdown
     - **Auto-invoke is allowlist-bounded, not heuristic.** A subtask that is nothing but a first-party slash command is executed; everything else is printed. The rule converts a string from a markdown file into an executed command, so the boundary is a fixed allowlist plus explicit disqualifiers — see § Subtask classification. Sandboxing what an allowlisted command does once invoked is the existing permission model's job and is unchanged here.
     ```

7. Do NOT change any other behavior of this command: refusal cases (step 3), the status entry contract (step 4), the 4 hard non-negotiables (step 5), and the phase transition (step 6) all stay exactly as they are. The `✅ All subtasks complete — run /vault-cli:complete-task` branch and the "omit the DoD block when absent" rule also stay as they are.

8. Do NOT apply the same change to `/workspace/commands/execute-goal.md`, `/workspace/commands/work-on-task.md`, `/workspace/commands/plan-task.md`, or any other command file. This spec scopes the change to `execute-task` only.

9. Add a second bullet to the `## Unreleased` section of `/workspace/CHANGELOG.md`, describing this user-visible change (do not modify or merge the resume-path bullet added by prompt 1):

   ```
   - feat: `/vault-cli:execute-task` now invokes the first unchecked subtask when it is a bare first-party slash-command call (e.g. ``Run `/start-day` ``) instead of printing it and waiting for an approval turn. Auto-invoke is bounded by an allowlist — shell commands, file paths, URLs, mixed prose, and multi-command lines are always printed, never run
   ```
</requirements>

<constraints>
- Markdown-only prompt. Do NOT modify any `.go` file, and do NOT modify `/workspace/mocks/`.
- Auto-invoke applies **only** to a subtask whose normalized content is a single allowlisted slash-command token (plus safe arguments). Surrounding prose, shell metacharacters in arguments, or a second command on the line disqualifies it — print it.
- The allowlist is limited to first-party Claude Code slash commands (`/vault-cli:*`, `/dark-factory:*`, `/coding:*`, and the bare-name commands/skills shipped with them). Bash, file paths, and URLs are never auto-invoked.
- Do NOT add a config flag, environment variable, or `--no-auto-invoke` style opt-out. The allowlist is the boundary; an opt-out on the behavior this spec ships would reintroduce the approval turn.
- Do NOT change `/workspace/commands/work-on-task.md` — its Phase 5 non-interactive branch is load-bearing for turn 1 and the spec requires it to stay byte-identical.
- Out of scope: sandboxing what an allowlisted command itself does once invoked. That is the existing permission model's job and is unchanged here.
- This repo is `autoRelease: true` (`.maintainer.yaml`). Add the `## Unreleased` bullet only — do NOT hand-bump `.claude-plugin/plugin.json` or `.claude-plugin/marketplace.json`, and do NOT `git tag`.
- Do NOT commit — dark-factory handles git.
- Existing tests must still pass.
</constraints>

<verification>
Run from `/workspace`:

Exactly one classification section, at line start:

```
grep -c '^### Subtask classification' commands/execute-task.md
```

Must print exactly `1`.

The two classification outcomes are spelled as the acceptance criteria expect:

```
grep -c 'bare slash-command call → invoke' commands/execute-task.md
grep -c 'anything else → print' commands/execute-task.md
```

Each must print a number `>= 1`.

The allowlist boundary and the disqualifiers are stated:

```
grep -c 'never auto-invoke' commands/execute-task.md
grep -c 'disqualifies' commands/execute-task.md
grep -n '/vault-cli:\*' commands/execute-task.md
grep -n '/dark-factory:\*' commands/execute-task.md
grep -n '/coding:\*' commands/execute-task.md
```

The two counts must each be `>= 1`; the three `grep -n` calls must each print at least one line.

Step 7 consumes the rule instead of only defining it:

```
grep -c 'Subtask classification' commands/execute-task.md
```

Must print a number `>= 2`.

`Skill` is available to the command:

```
grep -n 'allowed-tools' commands/execute-task.md
```

Must show `Skill` in the list.

No Go file and no sibling command was touched:

```
md5sum commands/work-on-task.md commands/execute-goal.md commands/plan-task.md
```

`commands/work-on-task.md` must be exactly `003d95631c482d5edc7333205fff34c6`. Record the other two hashes before making any edit and confirm they are unchanged after — this prompt touches only `commands/execute-task.md` and `CHANGELOG.md`, no sibling command and no Go file.

**Do not use git in this rung.** `hideGit=true` is required for worktree runs and masks `/workspace/.git` as a character device, so `git diff --name-only` / `git diff --stat` fail inside the container while passing on the host. Diff-based confirmation runs host-side in the spec's operator rung.

Changelog: both bullets present, exactly once each:

```
grep -c 'invokes the first unchecked subtask' CHANGELOG.md
grep -A10 '^## Unreleased' CHANGELOG.md
```

The count must be exactly `1`, and the section must show both the `- fix:` resume bullet and the `- feat:` execute-task bullet.

Then run the full gate once:

```
make precommit
```

Must exit 0. If it fails, fix and re-run only the failing target until green, then re-run `make precommit` once.

Note: `make precommit` cannot exercise a slash-command definition — the classification rule runs inside Claude Code, not in Go. The end-to-end evidence is the spec's Rung-3 criterion, replayed by the operator after merge and plugin update.
</verification>
